{# Sablona aplikace #}
{% extends 'base.html.twig'%}
{% form_theme form 'form/tapaForm.html.twig' %}
{# Titulek #}
{% block title %}Nová rezervace{% endblock %}
{# Obsah #}
{% block contenido %}
    <br><br>
    <div class="container">

        <h1 class="mt-4 mb-3 page-header">Nová rezervace</h1>
        <div class="row">
            <div class="col-md-8">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
